#include <stdio.h>
#include <stdint.h>
#include <locale.h>

int main(void)
{
    setlocale(LC_ALL, "RUS");
    
    for (int i = 0; i <= 255; i++)
    {
        printf("%c -- %i\n", i, i);
    }
    printf("\n");
	
    return 0;
}
